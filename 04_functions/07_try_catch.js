// try : write risky piece of code  (may cause error)
// catch : handle the execptions

// a =10;

try{
    console.log(a);
} catch{
    console.log("caught an error!")
}

console.log(" hello ")