const func = (arg1 , arg2) => {
    // function defination
}

const sum = (a , b) =>{
    return (a + b);
}
console.log(sum(3,5));
console.log(sum);

const cube = (n) => {
    return(n**3);
}

console.log(cube(5));


// Implicit Return in arrow functions 
// used for the functions which just return a value.

const mul = (a,b) =>(
    a * b 
);

